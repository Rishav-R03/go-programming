version: '3'

services:
  api:
    build: . 
    ports:
      - "3000:3000"
    env_file:
      - .env
    depends_on:
      - db 
  db:
    build: ./api/database 
    ports:
      - "6379:6379"
    volumes:
      - .data:/data 
